# GitHub Secret Scanner Pro - Список зависимостей
# Команда установки: pip install -r requirements.txt
#
# Важное замечание:
#   pip install aiohttp[speedups] aiodns
#   speedups включает cchardet и aiodns, что значительно повышает производительность

# Клиент GitHub API
PyGithub>=2.1.1

# Асинхронный HTTP-клиент (высокопроизводительное ядро)
# [speedups] включает: cchardet, aiodns, brotli
aiohttp[speedups]>=3.9.0
aiodns>=3.1.0

# Асинхронный DNS-резолвер (оптимизация для сети Китая)
# aiodns зависит от pycares
pycares>=4.0.0

# Rich TUI интерфейс
rich>=13.7.0

# Интерактивный TUI (вкладки, сортировка, мышь)
textual>=0.47.0

# Копирование ключей в буфер из TUI (опционально, без него — fallback)
pyperclip>=1.8.0

# Мета-процессы: stale-reaper сканера/TUI + мем-статистика (опционально)
# Без psutil функции тихо возвращают 0 (деградация, не падение).
psutil>=5.9.0

# Цветной вывод журнала
loguru>=0.7.2

# Механизм повторных попыток
urllib3>=2.0.0

# Поддержка SSL (HTTPS-прокси)
certifi>=2023.0.0

# ========== Новые зависимости ==========
aiosqlite>=0.19.0
uvloop>=0.19.0; sys_platform \!= 'win32'

# ========== Зависимости системы уведомлений v2.0 ==========
# Разбор YAML-файлов конфигурации
PyYAML>=6.0.0

# ========== Новые зависимости оптимизированной версии v2.0 ==========
# Функция шифрованного экспорта
cryptography>=41.0.0
